# Backend API base URL (no trailing slash). All API calls use only this.
# Local backend on port 8000:
# VITE_API_BASE_URL=http://localhost:8000/api
# Or use proxy (same origin): VITE_API_BASE_URL=/api
VITE_API_BASE_URL=http://localhost:8000/api
