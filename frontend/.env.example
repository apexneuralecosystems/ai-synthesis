# Backend API base URL (no trailing slash).
# Use /api when frontend is served with a proxy to the backend (same origin).
# Use full URL when backend is on another host (e.g. https://api.example.com/api).
VITE_API_BASE_URL=/api
