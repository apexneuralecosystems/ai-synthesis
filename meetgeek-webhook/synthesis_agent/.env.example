# Anthropic API key (primary LLM)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key (fallback LLM)
OPENAI_API_KEY=sk-proj-...

# CORS: comma-separated origins (no trailing slash; browser sends origin without path).
# CORS_ORIGINS=https://synthesis.apexneurallabs.com
